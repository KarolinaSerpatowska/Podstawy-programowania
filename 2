#include <stdio.h>
#include <stdlib.h>

int suma(int a, int b)
{
    return a+b;
}

int mnozenie(int a, int b)
{
    return a*b;
}

int porownaj(void  *a, void *b)
{
    int *w_a=(int*)a;
    int *w_b=(int*)b;
    return (*w_a<*w_b?-1:(*w_a==*w_b)?0:1)


   // if (*a<*b) return -1;
  //  else if (*a==*b) return 0;
  //  else return 1;
    
}

int zrob(int a, int b, int (*f)(void*, void*))
{
    printf("%d", f(&a,&b));
}


int main()
{
  //  int tab[]={1,2,3,4,5};
   // int (*f)(int,int);
  //  f=suma;//f=mnozenie;
  //  printf("%d", f(2,5));
    
    
  //  int tab[]={1,2,3,4,5};
 //   int (*f[2])(int,int); //tablica wskaznikow
 //   f[0]=suma;//f=mnozenie;
 //   f[1]=mnozenie;
 //   int i=0, wynik=0;
  //  for (i=0;i<5;i++)
  //  {
  //      wynik+=f[i%2](wynik,tab[i]);
 //   }
    
  //  printf("%d", wynik);
    
    int a=10;
    int b=20;
    zrob(a,b,porownaj); //przekazanie funkcji
    
    
    return 0;
}
