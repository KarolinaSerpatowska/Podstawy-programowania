#include <stdio.h>

void funkcja(int x)
{
    int i,suma=0;
    for (i=x; i!=(x-3);i--)
    {
        suma=suma+i;
    }
    printf("%d", suma);
}


int main()
{
    int x;
    scanf("%d", &x);
    funkcja(x);
    return 0;
}
