#include <stdio.h>

int main()
{
   int x,y;
   float wynik;
   scanf("%d", &x);
   scanf("%d", &y);

   if (x<0)
        x=x*(-1);
   else if (y<0)
    y=y*(-1);
    wynik=(1.0*(x+y))/2;
    printf("%f", wynik);

    return 0;
}
