#include <stdio.h>

int funkcja(int n)
{
    if (n==0) return 1;
    else return 2*funkcja(n-1);
   
}

int main()
{
    int x;
    scanf("%d", &x);
    printf("%d", funkcja(x));
    
    return 0;
}
