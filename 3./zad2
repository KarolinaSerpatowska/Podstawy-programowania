#include <stdio.h>

void dzielnik (int a)
{
    for (int i=1; i<=a; i++)
    {
        if (a%i==0)
        {
            printf ("%d ",i);
        }
    }
}

int main()
{
    int x;
    scanf("%d", &x);
    dzielnik(x);
  
    return 0;
}
