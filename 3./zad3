#include <stdio.h>

int suma (int n)
{
    int suma=0;
    for (int i=0; i<=n; i++)
    {
        if (i%2!=0) suma=suma+i;
            
    }
    return suma;
}




int main()
{
    int x;
    scanf("%d", &x);
    int wynik=suma(x);
    printf ("%d", wynik);
  
    return 0;
}
