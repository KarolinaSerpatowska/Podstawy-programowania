#include <stdio.h>
#include <string.h>

int main()
{
    char a[15];
    fgets(a,15, stdin);
    int i;
    for (i=0; i<15; i++)
    {
        if (a[i]>=97 && a[i]<=122)
        {
            a[i]=a[i]-32;
        }
    }
    printf("%s", a);
    
    return 0;
}
